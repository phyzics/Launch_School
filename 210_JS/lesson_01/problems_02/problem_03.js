// problem_03.js

var apples = 3;
var bananas = '3';

if (apples === bananas) {
  console.log("They're equal!");
}

// It doesn't output a message because we used the strict comparison operator
// and thus no type conversion was carried out
