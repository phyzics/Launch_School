# easy_110.rb
class Bag
  def initialize(color, material)
    @color = color
    @material = material
  end
end

# the `Object#new` method (make sure to specify the arguments)

Bag.new("Reasors", ['apple', 'diapers', 'coffee'])
