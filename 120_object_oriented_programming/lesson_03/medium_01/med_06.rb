# med_06.rb
=begin
- the second version uses the keyword self, thus explicitly calling the methods
  upon the instance. This is done for the getter in `show_template` in set 1,
  though self isn't used, but we set the instance variable manually rather than
  utilizing the method given to us by the attr_accessor
=end
