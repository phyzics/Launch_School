# practice08.rb
arr = [1, 2, 3, 4, 5]
arr.take(2)

# => Array#take returns the first 'n' elements of an array where n is an integer
# => passed through as an argument. Since it returns the elements in a new
# => array, it is not destructive. We can find out by a) reading the
# => documentation, and b) trying it out in irb
